import 'package:flutter/material.dart';

class SwipesListEmpty extends StatelessWidget {
  const SwipesListEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("It is all for now"),
    );
  }
}
